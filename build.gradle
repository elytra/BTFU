plugins {
    id 'idea'
}

idea {
    module {
        excludeDirs += [file("run")]
    }
}

subprojects {
    apply plugin: "idea"
    apply plugin: "scala"

    idea {
        module {
            excludeDirs += [file("run")]
        }
    }
}
